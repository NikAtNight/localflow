<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- LaunchAgent (registered via SMAppService.agent) instead of a plain
	     login item: KeepAlive below makes launchd relaunch LocalFlow after a
	     crash, while a clean quit (exit 0) stays quit. -->
	<key>Label</key>
	<string>app.talix.localflow</string>
	<key>BundleProgram</key>
	<string>Contents/MacOS/LocalFlow</string>
	<key>RunAtLoad</key>
	<true/>
	<key>KeepAlive</key>
	<dict>
		<key>SuccessfulExit</key>
		<false/>
	</dict>
	<key>ProcessType</key>
	<string>Interactive</string>
	<!-- NSLog bodies are privacy-redacted in `log show`; a plain file keeps
	     the app's own diagnostics readable. Note: transcripts appear in the
	     end-to-end latency lines, so this file contains dictated text. -->
	<key>StandardOutPath</key>
	<string>/Users/nikhlkapadia/Library/Logs/LocalFlow.log</string>
	<key>StandardErrorPath</key>
	<string>/Users/nikhlkapadia/Library/Logs/LocalFlow.log</string>
</dict>
</plist>
